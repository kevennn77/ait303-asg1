---
phase: 02-svm-sentiment-models
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [svm_sentiment_models.ipynb]
autonomous: true
requirements: [SENT-04, SENT-08, SENT-11, SENT-12]
user_setup: []

must_haves:
  truths:
    - "TfidfVectorizer features extracted from stemmed text with n-gram range (1,2)"
    - "TfidfVectorizer features extracted from lemmatized text with n-gram range (1,2)"
    - "SVM + TfidfVectorizer model trained on stemmed text"
    - "SVM + TfidfVectorizer model trained on lemmatized text"
    - "Both TfidfVectorizer models evaluated with 5-fold Stratified K-Fold cross-validation"
    - "All 4 SVM variants compared in a consolidated results table"
    - "Best overall SVM variant identified and justified"
  artifacts:
    - path: "svm_sentiment_models.ipynb"
      provides: "Complete SVM notebook with all 6 sections and all 4 model variants"
      min_cells: 34
    - path: "data/preprocessed_imdb.csv"
      provides: "Cached preprocessed DataFrame (unchanged from Plan 1)"
  key_links:
    - from: "svm_sentiment_models.ipynb"
      to: "data/preprocessed_imdb.csv"
      via: "pd.read_csv"
      pattern: "read_csv.*preprocessed_imdb"
    - from: "Section 6 Results Summary"
      to: "Section 5 Cross-Validation cell outputs"
      via: "comparison table references CV + test metrics"
      pattern: "accuracy|precision|recall|f1"
---

<objective>
Add TfidfVectorizer-backed SVM model variants to `svm_sentiment_models.ipynb` (Sections 3-5), train and evaluate both stemmed and lemmatized variants with 5-fold Stratified K-Fold CV, then produce a consolidated 4-model comparison table with all metrics and confusion matrices.

Purpose: Complete the second SVM vertical slice — TfidfVectorizer models — then consolidate all 4 variants (CountVectorizer × 2 + TfidfVectorizer × 2) into a single comparison, identifying the best overall model for the report.
Output: `svm_sentiment_models.ipynb` with TfidfVectorizer sections added and full 4-model comparison table present.
</objective>

<execution_context>
@/Users/keven/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keven/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-svm-sentiment-models/02-CONTEXT.md
@svm_sentiment_models.ipynb
@.planning/STATE.md

<interfaces>
Plan 1 established the following in `svm_sentiment_models.ipynb`:

**DataFrames available after Section 2:**
  - `df` — full preprocessed DataFrame with columns: review, sentiment, cleaned, stemmed, lemmatized, sentiment_encoded
  - `y` — binary encoded sentiment (1=positive, 0=negative)
  - `X_train_stemmed`, `X_test_stemmed` — stemmed text splits
  - `X_train_lemmatized`, `X_test_lemmatized` — lemmatized text splits
  - `y_train`, `y_test` — encoded label splits

**Existing fitted models after Plan 1 (Section 5):**
  - `pipeline_cv_stemmed` — fitted Pipeline(CountVectorizer(ngram_range=(1,2)) + SVC(kernel='linear', C=1.0))
  - `pipeline_cv_lemmatized` — fitted Pipeline(CountVectorizer(ngram_range=(1,2)) + SVC(kernel='linear', C=1.0))

**Existing CV results (Section 5):**
  - CV scores computed and displayed for both CountVectorizer variants

**Existing evaluation (Section 5-6):**
  - Test-set metrics, confusion matrices, top features, and summary for CountVectorizer models

All new cells must be inserted at appropriate positions within Sections 3-6, after the existing CountVectorizer content.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TfidfVectorizer feature extraction and SVM training with 5-fold cross-validation</name>
  <files>svm_sentiment_models.ipynb</files>
  <read_first>
    @svm_sentiment_models.ipynb
    @.planning/phases/02-svm-sentiment-models/02-CONTEXT.md
  </read_first>
  <action>
    Insert the following cells into `svm_sentiment_models.ipynb` within the existing structure. Place TfidfVectorizer content immediately after the CountVectorizer cross-validation cells (after Cell 14 from Plan 1, which fits the CountVectorizer Pipelines).

    **Section 3 (continued) — TfidfVectorizer Feature Extraction**
    - Cell 15 (markdown): Subsection "3.2 Feature Extraction with TfidfVectorizer". Use a clear header that differentiates from the CountVectorizer subsection.
    - Cell 16 (code): Define `TfidfVectorizer(ngram_range=(1,2))` per D-11. Fit on `X_train_stemmed` and transform both train/test. Fit on `X_train_lemmatized` and transform both train/test. Print vocabulary sizes. Store as `X_train_tfidf_stemmed`, `X_test_tfidf_stemmed`, `X_train_tfidf_lemmatized`, `X_test_tfidf_lemmatized`.

    **Section 4 (continued) — TfidfVectorizer Model Training**
    - Cell 17 (markdown): Subsection "4.3 SVM + TfidfVectorizer (Stemmed)" and "4.4 SVM + TfidfVectorizer (Lemmatized)".
    - Cell 18 (code): Define two new Pipeline objects following the same pattern as Plan 1, but using TfidfVectorizer instead of CountVectorizer:
      - `pipeline_tfidf_stemmed = Pipeline([('vectorizer', TfidfVectorizer(ngram_range=(1,2))), ('svm', SVC(kernel='linear', C=1.0, random_state=42))])`
      - `pipeline_tfidf_lemmatized = Pipeline([('vectorizer', TfidfVectorizer(ngram_range=(1,2))), ('svm', SVC(kernel='linear', C=1.0, random_state=42))])`
      Both Pipelines are unfitted — fitting occurs during cross-validation.

    **Section 5 (continued) — TfidfVectorizer Cross-Validation**
    - Cell 19 (markdown): Subsection "5.3 TfidfVectorizer Models (Cross-Validation)".
    - Cell 20 (code): Reuse the existing `cv = StratifiedKFold(n_splits=5, shuffle=True, random_state=42)` object from Plan 1.
      For each new Pipeline: `cross_validate(pipeline, X_train_text, y_train, cv=cv, scoring=['accuracy', 'precision', 'recall', 'f1'], return_train_score=True, n_jobs=-1)`.
      Collect CV scores for both TfidfVectorizer variants. Print mean ± std for each metric. Display a formatted comparison table of stemmed vs lemmatized TfidfVectorizer CV performance.
    - Cell 21 (code): Fit both TfidfVectorizer Pipelines on full training data: `pipeline_tfidf_stemmed.fit(X_train_stemmed, y_train)`, `pipeline_tfidf_lemmatized.fit(X_train_lemmatized, y_train)`. Print confirmation.
  </action>
  <acceptance_criteria>
    - Cells 15-21 added to the notebook (total ≥ 22 cells after insertions)
    - TfidfVectorizer(ngram_range=(1,2)) defined and fitted on stemmed and lemmatized text
    - Two new Pipelines defined with TfidfVectorizer + SVC(kernel='linear', C=1.0)
    - cross_validate runs on both new Pipelines with same cv object and scoring metrics
    - Mean ± std CV scores printed for both TfidfVectorizer variants
    - Both new Pipelines fitted on full training data
    - All existing CountVectorizer cells remain intact
  </acceptance_criteria>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute svm_sentiment_models.ipynb --output /dev/null --ExecutePreprocessor.timeout=600 2>&1 | grep -c "ERROR"</automated>
    <expected>0 — notebook executes end-to-end with both CountVectorizer and TfidfVectorizer cells</expected>
  </verify>
  <done>TfidfVectorizer features extracted, both SVM + TfidfVectorizer Pipelines cross-validated and fitted on full training data</done>
</task>

<task type="auto">
  <name>Task 2: Consolidated 4-model evaluation, comparison table, and final results</name>
  <files>svm_sentiment_models.ipynb</files>
  <read_first>
    @svm_sentiment_models.ipynb
  </read_first>
  <action>
    Insert the following cells after the TfidfVectorizer model fitting (after Cell 21 from Task 1).

    **Test-Set Evaluation — TfidfVectorizer Models**
    - Cell 22 (markdown): Subsection "5.4 Test-Set Evaluation — TfidfVectorizer Models".
    - Cell 23 (code): For each fitted TfidfVectorizer Pipeline, predict on the corresponding test set. Compute and display:
      - `accuracy_score`, `precision_score(macro)`, `recall_score(macro)`, `f1_score(macro)` per D-15
      - `classification_report` with target names ['negative', 'positive']
      - `confusion_matrix` displayed as both raw array and seaborn heatmap (annotated, `cmap='Blues'`, labels ["negative", "positive"])
    - Cell 24 (markdown): Subsection "5.5 Most Informative Features — TfidfVectorizer Models".
    - Cell 25 (code): For each fitted TfidfVectorizer Pipeline, extract coefficients (`pipeline.named_steps['svm'].coef_[0]`) and feature names (`pipeline.named_steps['vectorizer'].get_feature_names_out()`). Display top 10 positive and top 10 negative features for both stemmed and lemmatized TfidfVectorizer variants as formatted tables.

    **Consolidated 4-Model Comparison**
    - Cell 26 (markdown): Section header "5.6 All 4 Models — Consolidated Comparison".
    - Cell 27 (code): Build a single DataFrame consolidating test-set metrics for all 4 variants:
      - Rows: 4 variants (CV+Stemmed, CV+Lemmatized, TFIDF+Stemmed, TFIDF+Lemmatized)
      - Columns: Accuracy, Precision (macro), Recall (macro), F1-Score (macro)
      - Also include CV mean F1 as an additional column
      - Use `pd.DataFrame` with explicit index and values from previously computed metrics
      - Display with a styled table (highlight best score per column in bold)
    - Cell 28 (code): Print identification of the best overall model variant based on test F1-score. Include a brief comment on which preprocessing strategy and vectorizer performed best.

    **Section 6 — Final Results Summary (All 4 Models)**
    - Cell 29 (markdown): Section header "6. Results Summary" per D-18.
    - Cell 30 (code): Final summary markdown/print block that presents:
      - All 4 models ranked by test F1-score
      - Cross-validation F1 mean for each variant (inline reference)
      - Best CountVectorizer variant identified
      - Best TfidfVectorizer variant identified
      - Overall best SVM model with justification (vectorizer type + preprocessing strategy that won)
      - Key takeaways for the report's model comparison section

    **Notebook Cleanup**
    - Verify all section headers follow D-18 numbering: "1. Setup & Imports", "2. Load Preprocessed Data", "3. Feature Extraction", "4. Model Training", "5. Cross-Validation & Evaluation", "6. Results Summary"
    - Ensure subsection headers use consistent decimal numbering (e.g., "5.1 CountVectorizer Models", "5.2 Test-Set Evaluation — CountVectorizer", "5.3 TfidfVectorizer Models", "5.4 Test-Set Evaluation — TfidfVectorizer", "5.5 Most Informative Features", "5.6 All 4 Models — Consolidated Comparison")
  </action>
  <acceptance_criteria>
    - Cells 22-30 added to the notebook (total ≥ 30 cells)
    - TfidfVectorizer test-set accuracy, precision, recall, F1 computed and displayed
    - Confusion matrices as seaborn heatmaps for both TfidfVectorizer variants
    - Top 10 positive/negative features displayed for both TfidfVectorizer variants
    - Consolidated 4-model comparison DataFrame exists with rows: CV+Stemmed, CV+Lemmatized, TFIDF+Stemmed, TFIDF+Lemmatized and columns: Accuracy, Precision, Recall, F1, CV_Mean_F1
    - Best overall model identified and printed with justification
    - Section 6 contains final ranked comparison of all 4 variants
    - All section headers match D-18 specification
  </acceptance_criteria>
  <verify>
    <automated>
      # 1. Full notebook execution
      jupyter nbconvert --to notebook --execute svm_sentiment_models.ipynb --output /dev/null --ExecutePreprocessor.timeout=600 2>&1 | grep -c "ERROR"
      # 2. Verify cell count ≥ 30
      python3 -c "import json; nb=json.load(open('svm_sentiment_models.ipynb')); cells=[c for c in nb['cells'] if c['cell_type']=='code']; print(f'{len(cells)} code cells')"
      # 3. Verify 4-model comparison table exists in output
      grep -c "TFIDF+Stemmed\|TFIDF+Lemmatized\|CV+Stemmed\|CV+Lemmatized" svm_sentiment_models.ipynb
    </automated>
    <expected>0 errors, ≥ 30 code cells, ≥ 1 mention of all 4 variant names</expected>
  </verify>
  <done>All 4 SVM variants fully evaluated, consolidated comparison table produced, best overall model identified and justified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notebook environment | All code runs locally in Jupyter kernel. No network requests. Same trust boundary as Plan 1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | TfidfVectorizer fitted objects | mitigate | Validate that TfidfVectorizer is separate from CountVectorizer — two distinct Pipeline objects per text variant, not accidental reuse of fitted vectorizer |
| T-02-05 | Information Disclosure | Notebook output cells | accept | Same as Plan 1 — public dataset, no PII |
| T-02-06 | Repudiation | Metric comparison | mitigate | All 4 variants evaluated on same train/test split (y_train, y_test from Plan 1). Comparison table references shared splits ensuring fair comparison. |
| T-02-SC | Tampering | pip package installs | mitigate | No new packages beyond Plan 1. TfidfVectorizer is from sklearn.feature_extraction.text already imported. |
</threat_model>

<verification>
- [ ] All 4 SVM variants (CV+stemmed, CV+lemmatized, TFIDF+stemmed, TFIDF+lemmatized) trained and evaluated
- [ ] `jupyter nbconvert --to notebook --execute svm_sentiment_models.ipynb --output /dev/null --ExecutePreprocessor.timeout=600` exits with code 0
- [ ] TfidfVectorizer vocabulary size printed in output
- [ ] Both TfidfVectorizer variants have CV scores displayed with mean ± std
- [ ] Confusion matrices rendered as seaborn heatmaps for all 4 variants
- [ ] Consolidated comparison table has all 4 rows and 5 score columns
- [ ] Best overall model printed with justification
- [ ] All 6 sections per D-18 present with correct numbering
</verification>

<success_criteria>
- TfidfVectorizer features extracted from both stemmed and lemmatized text with ngram_range=(1,2) (SENT-04 ✓)
- SVM + TfidfVectorizer models trained for both stemmed and lemmatized variants (SENT-08 ✓)
- 5-fold Stratified K-Fold cross-validation applied to both TfidfVectorizer variants (SENT-11 ✓)
- Test-set accuracy, precision, recall, F1, and confusion matrix reported for all 4 variants (SENT-12 ✓)
- Consolidated 4-model comparison table produced
- Best overall SVM model identified with justification
- All cells execute without errors in nbconvert
</success_criteria>

<output>
Create `.planning/phases/02-svm-sentiment-models/02-02-SUMMARY.md` when done
</output>
