---
phase: 02-svm-sentiment-models
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [svm_sentiment_models.ipynb]
autonomous: true
requirements: [SENT-03, SENT-07, SENT-11, SENT-12]
user_setup: []

must_haves:
  truths:
    - "CountVectorizer features extracted from stemmed text with n-gram range (1,2)"
    - "CountVectorizer features extracted from lemmatized text with n-gram range (1,2)"
    - "SVM + CountVectorizer model trained on stemmed text"
    - "SVM + CountVectorizer model trained on lemmatized text"
    - "Both CountVectorizer models evaluated with 5-fold Stratified K-Fold cross-validation"
    - "Test-set accuracy, precision, recall, F1, and confusion matrix reported for both variants"
    - "Top positive/negative features shown from linear SVM coefficients"
  artifacts:
    - path: "svm_sentiment_models.ipynb"
      provides: "Complete SVM CountVectorizer notebook with Sections 1-6"
      min_cells: 14
    - path: "data/preprocessed_imdb.csv"
      provides: "Cached preprocessed DataFrame for reuse across Phase 2 plans"
      min_lines: 2
  key_links:
    - from: "svm_sentiment_models.ipynb"
      to: "data/preprocessed_imdb.csv"
      via: "pd.read_csv"
      pattern: "read_csv.*preprocessed_imdb"
    - from: "svm_sentiment_models.ipynb"
      to: "sentiment_analysis_preprocessing.ipynb"
      via: "%run or CSV cache"
      pattern: "preprocessed_imdb\\.csv|%run.*preprocessing"
---

<objective>
Create `svm_sentiment_models.ipynb` with Sections 1–6 (per D-18), implement CountVectorizer feature extraction on both stemmed and lemmatized text, train SVM + CountVectorizer model variants, and evaluate both with 5-fold Stratified K-Fold cross-validation plus test-set metrics.

Purpose: Deliver the first SVM vertical slice — CountVectorizer-backed models for both preprocessing strategies — with full cross-validation and evaluation so results are immediately usable for the 4-model comparison.
Output: `svm_sentiment_models.ipynb` with the first 6 notebook sections, cached preprocessed CSV.
</objective>

<execution_context>
@/Users/keven/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keven/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-svm-sentiment-models/02-CONTEXT.md
@sentiment_analysis_preprocessing.ipynb
@.planning/phases/01-data-preparation-preprocessing/SKELETON.md
@.planning/STATE.md

<interfaces>
Phase 1 notebook produces `df` DataFrame with these columns after execution:
  - `review` (str) — raw review text
  - `sentiment` (str) — "positive" or "negative"
  - `cleaned` (str) — after lowercasing, HTML removal, punctuation removal
  - `stemmed` (str) — after tokenization, stopword removal, Porter stemming
  - `lemmatized` (str) — after tokenization, stopword removal, POS-tagged WordNet lemmatization

Label encoding: sentiment values are "positive"/"negative" strings — encode to binary (1/0) for sklearn.
Shape: (50000, 5), perfectly balanced (25000 positive, 25000 negative).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notebook scaffold, data loading, and train/test split</name>
  <files>svm_sentiment_models.ipynb</files>
  <read_first>
    @sentiment_analysis_preprocessing.ipynb
    @.planning/phases/02-svm-sentiment-models/02-CONTEXT.md
  </read_first>
  <action>
    Create `svm_sentiment_models.ipynb` as a new Jupyter Notebook (nbformat 4). Implement:

    **Section 1 — Setup & Imports**
    - Cell 1 (markdown): Notebook title "AIT303 Assignment 1 — SVM Sentiment Models", author placeholder, description of what this notebook does: trains 4 SVM variants using CountVectorizer and TfidfVectorizer on stemmed and lemmatized IMDB reviews.
    - Cell 2 (code): Imports — `pandas`, `numpy`, `matplotlib.pyplot`, `seaborn`, `sklearn.feature_extraction.text.CountVectorizer`, `sklearn.svm.SVC`, `sklearn.pipeline.Pipeline`, `sklearn.model_selection.StratifiedKFold`, `sklearn.model_selection.cross_val_score`, `sklearn.model_selection.cross_validate`, `sklearn.metrics.accuracy_score`, `sklearn.metrics.precision_score`, `sklearn.metrics.recall_score`, `sklearn.metrics.f1_score`, `sklearn.metrics.confusion_matrix`, `sklearn.metrics.classification_report`, `sklearn.model_selection.train_test_split`. Set `%matplotlib inline`. Fix random seed with `np.random.seed(42)`.

    **Section 2 — Load Preprocessed Data**
    - Cell 3 (markdown): Section header "2. Load Preprocessed Data".
    - Cell 4 (code): Check if `data/preprocessed_imdb.csv` exists. If yes, `pd.read_csv()` to load. If no, execute `%run sentiment_analysis_preprocessing.ipynb` (runs Phase 1 notebook in same kernel to produce `df`), then `df.to_csv('data/preprocessed_imdb.csv', index=False)` to cache. Print shape and columns to confirm.
    - Cell 5 (code): Encode sentiment labels — `df['sentiment'].map({'positive': 1, 'negative': 0})`. Print class distribution to confirm balance. Define `y = df['sentiment_encoded']`.

    **Train/Test Split**
    - Cell 6 (markdown): Subsection "Train/Test Split" (per D-16: 80/20 split).
    - Cell 7 (code): `train_test_split` with `test_size=0.2`, `random_state=42`, `stratify=y` to preserve class distribution. Produce four arrays: `X_train_stemmed`, `X_test_stemmed` (from `df['stemmed']`), `X_train_lemmatized`, `X_test_lemmatized` (from `df['lemmatized']`), plus `y_train`, `y_test`. Print split sizes.
  </action>
  <acceptance_criteria>
    - Notebook file exists at `svm_sentiment_models.ipynb`
    - Notebook contains 7 cells (2 markdown + 5 code) in Sections 1-2
    - Cell 4 executes without error and either loads CSV or runs Phase 1 notebook
    - Cell 7 produces train (40000) and test (10000) splits with balanced class distribution
    - `data/preprocessed_imdb.csv` exists after first execution
  </acceptance_criteria>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute svm_sentiment_models.ipynb --output /dev/null --ExecutePreprocessor.timeout=600 2>&1 | grep -c "ERROR"</automated>
    <expected>0 — notebook executes end-to-end without errors</expected>
  </verify>
  <done>Notebook Sections 1-2 executed successfully, preprocessed data cached to CSV, train/test split produced</done>
</task>

<task type="auto">
  <name>Task 2: CountVectorizer feature extraction, SVM training, and 5-fold cross-validation</name>
  <files>svm_sentiment_models.ipynb</files>
  <read_first>
    @svm_sentiment_models.ipynb
    @.planning/phases/02-svm-sentiment-models/02-CONTEXT.md
  </read_first>
  <action>
    Append the following to `svm_sentiment_models.ipynb`:

    **Section 3 — Feature Extraction (CountVectorizer)**
    - Cell 8 (markdown): Section header "3. Feature Extraction with CountVectorizer" per D-18.
    - Cell 9 (code): Define `CountVectorizer(ngram_range=(1,2))` per D-11. Fit on `X_train_stemmed` and transform both train/test. Fit on `X_train_lemmatized` and transform both train/test. Print vocabulary sizes. Store as `X_train_cv_stemmed`, `X_test_cv_stemmed`, `X_train_cv_lemmatized`, `X_test_cv_lemmatized`.

    **Section 4 — Model Training (CountVectorizer)**
    - Cell 10 (markdown): Section header "4. SVM Model Training" per D-18. Subsection "4.1 SVM + CountVectorizer (Stemmed)" and "4.2 SVM + CountVectorizer (Lemmatized)".
    - Cell 11 (code): Define two `Pipeline` objects using `sklearn.pipeline.Pipeline`:
      - `pipeline_cv_stemmed = Pipeline([('vectorizer', CountVectorizer(ngram_range=(1,2))), ('svm', SVC(kernel='linear', C=1.0, random_state=42))])` per D-12, D-13.
      - `pipeline_cv_lemmatized = Pipeline([('vectorizer', CountVectorizer(ngram_range=(1,2))), ('svm', SVC(kernel='linear', C=1.0, random_state=42))])`
      Note: Pipeline wraps vectorizer + classifier so cross-validation operates on the full feature extraction + training pipeline (per CONTEXT.md specifics). Both Pipelines are defined but not yet fitted — fitting happens inside cross-validation.

    **5-fold Stratified K-Fold Cross-Validation (Section 5)**
    - Cell 12 (markdown): Section header "5. Cross-Validation & Evaluation" per D-18. Subsection "5.1 CountVectorizer Models".
    - Cell 13 (code): Define `cv = StratifiedKFold(n_splits=5, shuffle=True, random_state=42)` per D-14.
      For each Pipeline: `cross_validate(pipeline, X_train_text, y_train, cv=cv, scoring=['accuracy', 'precision', 'recall', 'f1'], return_train_score=True, n_jobs=-1)`.
      Collect CV scores for both variants. Print mean ± std for each metric for both stemmed and lemmatized pipelines. Display as a formatted table comparing stemmed vs lemmatized CV performance.
    - Cell 14 (code): Fit both Pipelines on full training data for test-set evaluation: `pipeline_cv_stemmed.fit(X_train_stemmed, y_train)`, `pipeline_cv_lemmatized.fit(X_train_lemmatized, y_train)`. Print confirmation that fitting completed.
  </action>
  <acceptance_criteria>
    - Cells 8-14 exist in the notebook after Section 2
    - CountVectorizer vocabulary size is printed (typically 50k-120k terms)
    - Both Pipeline objects are defined with CountVectorizer(ngram_range=(1,2)) + SVC(kernel='linear', C=1.0)
    - StratifiedKFold(n_splits=5, shuffle=True, random_state=42) is defined
    - cross_validate runs on both Pipelines with scoring=['accuracy','precision','recall','f1'] and return_train_score=True
    - Mean ± std CV scores are printed for both stemmed and lemmatized variants
    - Both Pipelines are fitted on full training data
  </acceptance_criteria>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute svm_sentiment_models.ipynb --output /dev/null --ExecutePreprocessor.timeout=600 2>&1 | grep -c "ERROR"</automated>
    <expected>0 — all cells including expensive CV cells execute without errors</expected>
  </verify>
  <done>CountVectorizer features extracted, both SVM + CountVectorizer Pipelines trained, 5-fold Stratified K-Fold CV scores computed and displayed, both Pipelines fitted on full training set</done>
</task>

<task type="auto">
  <name>Task 3: Test-set evaluation, confusion matrices, top features, and results summary</name>
  <files>svm_sentiment_models.ipynb</files>
  <read_first>
    @svm_sentiment_models.ipynb
  </read_first>
  <action>
    Append the following cells to `svm_sentiment_models.ipynb`:

    **Test-Set Evaluation**
    - Cell 15 (markdown): Subsection "5.2 Test-Set Evaluation — CountVectorizer Models".
    - Cell 16 (code): For each fitted Pipeline, predict on the corresponding test set. Compute and display:
      - `accuracy_score`, `precision_score` (macro), `recall_score` (macro), `f1_score` (macro) per D-15
      - `classification_report` with target names
      - `confusion_matrix` — display as both raw array and seaborn heatmap (annotated, `cmap='Blues'`, with axis labels "Predicted" / "Actual", tick labels ["negative", "positive"])
    - Cell 17 (code): Create a comparison DataFrame/table that shows both stemmed and lemmatized CountVectorizer models side by side with all 4 metrics.
    - Cell 18 (markdown): Subsection "5.3 Most Informative Features" (per CONTEXT.md specifics about interpretability).

    **Top Features from Coefficients**
    - Cell 19 (code): For each fitted Pipeline, extract the SVM coefficients (`pipeline.named_steps['svm'].coef_[0]`) and the feature names from the fitted CountVectorizer (`pipeline.named_steps['vectorizer'].get_feature_names_out()`). Sort coefficients, display top 10 positive (most indicative of "positive") and top 10 negative (most indicative of "negative") features for both stemmed and lemmatized variants. Display as formatted tables with columns: feature name, coefficient value, sentiment association.

    **Section 6 — Results Summary (CountVectorizer)**
    - Cell 20 (markdown): Section header "6. Results Summary" per D-18. Subsection "6.1 CountVectorizer SVM Results".
    - Cell 21 (code): A final summary markdown/print block that displays:
      - Best CountVectorizer variant (stemmed or lemmatized) based on test F1-score
      - Cross-validation mean F1 for both variants
      - Test-set F1 for both variants
      - Observation about how preprocessing choice affected performance
      - Key takeaway for the report
  </action>
  <acceptance_criteria>
    - Cells 15-21 exist in the notebook (total ≥ 21 cells)
    - Test-set accuracy, precision, recall, F1 computed and printed for both variants
    - Confusion matrices displayed as both raw arrays and seaborn heatmaps for both variants
    - Side-by-side metrics comparison table exists for stemmed vs lemmatized
    - Top 10 positive and top 10 negative features displayed for both variants with coefficient values
    - Results summary section presents best CountVectorizer variant with F1 comparison
    - All metrics are consistent (accuracy, precision, recall, F1 all between 0 and 1)
  </acceptance_criteria>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute svm_sentiment_models.ipynb --output /dev/null --ExecutePreprocessor.timeout=600 2>&1 | grep -c "ERROR"</automated>
    <expected>0 — full notebook with all evaluation cells executes without errors</expected>
  </verify>
  <done>Both CountVectorizer SVM variants fully evaluated: test-set metrics computed, confusion matrices visualized, top informative features extracted, results summary presented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notebook environment | All code runs locally in Jupyter kernel. No network requests beyond Phase 1 notebook re-execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | data/preprocessed_imdb.csv | mitigate | Load with pd.read_csv; validate shape (50000, 5) and expected column names (review, sentiment, cleaned, stemmed, lemmatized) before proceeding |
| T-02-02 | Information Disclosure | Notebook output cells | accept | All data is public IMDB reviews — no PII or sensitive content. Output metrics and confusion matrices are required deliverables. |
| T-02-03 | Repudiation | Model training results | mitigate | Fix random_state=42 in all sklearn operations (train_test_split, StratifiedKFold, SVC) to ensure deterministic, reproducible results |
| T-02-SC | Tampering | pip/npm package installs | mitigate | All imports (pandas, numpy, sklearn, matplotlib, seaborn) are well-known, widely-used packages — [ASSUMED] legitimacy. No new packages introduced beyond Phase 1 stack. |
</threat_model>

<verification>
- [ ] `svm_sentiment_models.ipynb` created with ≥ 21 cells across Sections 1-6 per D-18
- [ ] `jupyter nbconvert --to notebook --execute svm_sentiment_models.ipynb --output /dev/null --ExecutePreprocessor.timeout=600` exits with code 0
- [ ] CountVectorizer vocabulary size printed in output (expected: 50k-120k terms)
- [ ] CV scores for both stemmed and lemmatized variants displayed with mean ± std
- [ ] Test-set confusion matrices rendered as seaborn heatmaps for both variants
- [ ] Top 10 positive and negative features extracted and displayed
- [ ] Results summary identifies best CountVectorizer variant
</verification>

<success_criteria>
- CountVectorizer features extracted from both stemmed and lemmatized text with ngram_range=(1,2) (SENT-03 ✓)
- SVM + CountVectorizer models trained for both stemmed and lemmatized variants (SENT-07 ✓)
- 5-fold Stratified K-Fold cross-validation applied to both variants (SENT-11 ✓)
- Test-set accuracy, precision, recall, F1, and confusion matrix reported (SENT-12 ✓)
- All cells execute without errors in nbconvert
- Top informative features displayed for model interpretability
</success_criteria>

<output>
Create `.planning/phases/02-svm-sentiment-models/02-01-SUMMARY.md` when done
</output>
