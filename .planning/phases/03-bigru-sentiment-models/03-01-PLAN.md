---
phase: 03-bigru-sentiment-models
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bigru_sentiment_models.ipynb
  - models/word2vec_cbow.model
  - models/word2vec_sg.model
autonomous: true
requirements:
  - SENT-05
  - SENT-06
user_setup:
  - service: google-colab
    why: "GPU training for 10 BiGRU models"
    env_vars: []
    dashboard_config:
      - task: "Upload data_asg/ folder to Google Drive"
        location: "drive.google.com"
        details: "The preprocessed_imdb.csv must be at /content/drive/MyDrive/data_asg/preprocessed_imdb.csv on Colab"
must_haves:
  truths:
    - "Word2Vec CBOW embeddings trained on lemmatized IMDB reviews with vector_size=100, window=5, min_count=5"
    - "Word2Vec Skip-Gram embeddings trained on same corpus with identical parameters"
    - "Both word2vec models saved to disk as .model files"
    - "Embedding matrix built from CBOW vocabulary with tokenizer coverage >80%"
    - "Embedding matrix built from Skip-Gram vocabulary with tokenizer coverage >80%"
    - "BiGRU model builder function defined and ready for CV training in Plan 02"
  artifacts:
    - path: "bigru_sentiment_models.ipynb"
      provides: "Complete notebook with sections 1-4"
      min_lines: 200
    - path: "models/word2vec_cbow.model"
      provides: "Trained CBOW word embeddings (gensim format)"
    - path: "models/word2vec_sg.model"
      provides: "Trained Skip-Gram word embeddings (gensim format)"
  key_links:
    - from: "bigru_sentiment_models.ipynb"
      to: "models/word2vec_cbow.model"
      via: "cbow_model.save()"
      pattern: "model\\.save"
    - from: "bigru_sentiment_models.ipynb"
      to: "models/word2vec_sg.model"
      via: "sg_model.save()"
      pattern: "model\\.save"
    - from: "bigru_sentiment_models.ipynb"
      to: "data_asg/preprocessed_imdb.csv"
      via: "pd.read_csv"
      pattern: "read_csv"
---

<objective>
**Create the BiGRU sentiment analysis notebook scaffold; train Word2Vec CBOW and Skip-Gram embeddings; build embedding matrices and BiGRU model factory.**

**Purpose:** Establish the complete foundation for Phase 3 — Colab environment, data loading, both Word2Vec embedding models, vocabulary, and model builder — so Plan 02 can run the 5-fold CV training loop.

**Output:** `bigru_sentiment_models.ipynb` (sections 1-4 complete), `models/word2vec_cbow.model`, `models/word2vec_sg.model`
</objective>

<execution_context>
@/Users/keven/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keven/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bigru-sentiment-models/CONTEXT.md
@.planning/phases/03-bigru-sentiment-models/03-RESEARCH.md
@.planning/phases/03-bigru-sentiment-models/03-PATTERNS.md
@svm_sentiment_models.ipynb
</context>

<interfaces>
<!-- From Phase 2 notebook — patterns to reuse verbatim -->

From svm_sentiment_models.ipynb — Colab setup pattern (lines 50-65):
- COLAB = True flag
- from google.colab import drive; drive.mount('/content/drive')
- DATA_DIR = '/content/drive/MyDrive/data_asg'
- Add MODEL_DIR = f'{DATA_DIR}/models'

From svm_sentiment_models.ipynb — data loading pattern (lines 327-335):
- pd.read_csv(f'{DATA_DIR}/preprocessed_imdb.csv')
- df has columns: review, sentiment, cleaned, stemmed, lemmatized (5 cols)

From svm_sentiment_models.ipynb — label encoding (lines 361-362):
- df['sentiment_encoded'] = df['sentiment'].map({'positive': 1, 'negative': 0})

From svm_sentiment_models.ipynb — StratifiedKFold (line 619):
- cv = StratifiedKFold(n_splits=5, shuffle=True, random_state=42)  # seed=42

From svm_sentiment_models.ipynb — evaluation function pattern (lines 738-773):
- Returns {accuracy, precision, recall, f1}
- Uses sns.heatmap for confusion matrix with xticklabels/yticklabels=['negative','positive']
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create notebook scaffold with Colab setup, imports, data loading, and reproducibility seeds</name>
  <files>bigru_sentiment_models.ipynb</files>

  <read_first>
  - svm_sentiment_models.ipynb (lines 1-119 for Colab setup + imports pattern; lines 327-374 for data loading + label encoding pattern)
  - CONTEXT.md (GPU Execution section: COLAB=True flag, Drive mount, DATA_DIR)
  - 03-RESEARCH.md lines 126-144 (notebook architecture diagram) and lines 208-239 (recommended notebook structure)
  - 03-PATTERNS.md lines 27-93 (Colab setup, imports, data loading, label encoding patterns)
  </read_first>

  <action>
  Create the notebook `bigru_sentiment_models.ipynb` with the exact structure below. Follow the Phase 2 notebook (`svm_sentiment_models.ipynb`) format exactly — same cell type ordering (Markdown → Code), same { } output placeholders (let Colab generate them), same print('='*50) separator style, same comment style (# ----).

  **Cell layout:**

  Cell 0 (Markdown): Title cell
  - Title: `# AIT303 Assignment 1 — BiGRU Sentiment Models`
  - Author placeholder, date
  - Brief: "Trains 2 BiGRU variants on the IMDB 50K dataset with Word2Vec embeddings"
  - Table showing the two models: CBOW-BiGRU and Skip-Gram-BiGRU
  - Colab instructions (same style as SVM notebook)

  Cell 1 (Code): Configuration cell
  - COLAB = True
  - if COLAB: from google.colab import drive; drive.mount('/content/drive'); DATA_DIR = '/content/drive/MyDrive/data_asg'; MODEL_DIR = f'{DATA_DIR}/models'
  - else: DATA_DIR = 'data_asg'; MODEL_DIR = 'models'
  - print(f"Running in ... mode"), print(f"Data directory: {DATA_DIR}"), print(f"Model directory: {MODEL_DIR}")

  Cell 2 (Markdown): `## 1. Setup & Imports`

  Cell 3 (Code): Imports cell
  Per D-16 (seeds), per CONTEXT.md locked decisions, and per RESEARCH.md standard stack:
  ```
  import pandas as pd
  import numpy as np
  import matplotlib.pyplot as plt
  import seaborn as sns
  %matplotlib inline
  
  import gensim
  from gensim.models import Word2Vec
  
  import tensorflow as tf
  from tensorflow.keras.layers import Embedding, Bidirectional, GRU, Dropout, Dense
  from tensorflow.keras.models import Sequential
  from tensorflow.keras.callbacks import EarlyStopping
  from tensorflow.keras.preprocessing.sequence import pad_sequences
  from tensorflow.keras.preprocessing.text import Tokenizer
  
  from sklearn.model_selection import StratifiedKFold, train_test_split
  from sklearn.metrics import (
      accuracy_score, precision_score, recall_score, f1_score,
      roc_auc_score, confusion_matrix, classification_report
  )
  
  import random
  np.random.seed(42)
  random.seed(42)
  tf.random.set_seed(42)
  
  print("All imports loaded successfully")
  ```
  All three seeds set per D-16. Precision_score averaging must be identical to Phase 2 — check the SVM notebook for which averaging was used. Phase 2 used weighted averaging (not macro).

  Cell 4 (Markdown): `## 2. Load Preprocessed Data`

  Cell 5 (Code): Data loading cell
  Identical to Phase 2 pattern (svm_sentiment_models.ipynb lines 327-335):
  - df = pd.read_csv(f'{DATA_DIR}/preprocessed_imdb.csv')
  - Print shape, columns, class distribution, missing values
  - df.head(3)

  Cell 6 (Code): Label encoding cell
  Identical to Phase 2 pattern (svm_sentiment_models.ipynb lines 361-374):
  - df['sentiment_encoded'] = df['sentiment'].map({'positive': 1, 'negative': 0})
  - Extract y = df['sentiment_encoded'].values
  - Extract lemmatized_texts = df['lemmatized'].values
  - Print: encoded distribution, shapes
  - NOTE: Phase 3 uses full dataset for 5-fold CV (no held-out test split). Unlike Phase 2 which reserved 20% for test, Phase 3 uses ALL data for StratifiedKFold.

  No output values should be embedded — mark outputs as empty {} and let Colab fill them.
  </action>

  <acceptance_criteria>
  - Notebook file exists at root level as `bigru_sentiment_models.ipynb`
  - File is valid JSON (nbformat >= 4)
  - Contains exactly 7 cells (2 markdown + 5 code) in correct order
  - Cell 1 has COLAB = True, Drive mount, DATA_DIR, MODEL_DIR
  - Cell 3 imports all required libs (pandas, numpy, gensim, tensorflow, sklearn, random)
  - Cell 3 sets np.random.seed(42), random.seed(42), tf.random.set_seed(42)
  - Cell 5 loads data from DATA_DIR/preprocessed_imdb.csv
  - Cell 6 encodes sentiment: positive→1, negative→0
  - Cell 6 extracts df['lemmatized'].values as lemmatized_texts
  - Cell 6 does NOT create a held-out test set (unlike Phase 2)
  </acceptance_criteria>

  <verify>
  <automated>python3 -c "import json; nb = json.load(open('bigru_sentiment_models.ipynb')); assert nb['nbformat'] >= 4; assert len(nb['cells']) == 7; src = ''.join(c['source'] for c in nb['cells'] if c['cell_type'] == 'code'); assert 'COLAB = True' in src; assert 'tf.random.set_seed(42)' in src; assert 'Word2Vec' in src; assert 'lemmatized' in src; print('PASS')"</automated>
  </verify>

  <done>Notebook scaffold with 7 cells created, imports ready, data loading and label encoding complete. No held-out test split — all data reserved for 5-fold CV.</done>
</task>

<task type="auto">
  <name>Task 2: Train Word2Vec CBOW + Skip-Gram embeddings, build vocabulary + embedding matrices, and define BiGRU model builder</name>
  <files>bigru_sentiment_models.ipynb, models/word2vec_cbow.model, models/word2vec_sg.model</files>

  <read_first>
  - CONTEXT.md (Word Embedding Config: sg=0 for CBOW, sg=1 for SG, vector_size=100, window=5, min_count=5 — all locked per D-01/D-02/D-03/D-04)
  - 03-RESEARCH.md lines 242-276 (Pattern 1: Build Embedding Matrix) and lines 280-313 (Pattern 2: BiGRU Model Builder)
  - 03-PATTERNS.md lines 137-209 (Word2Vec training pattern + embedding matrix pattern)
  - 03-RESEARCH.md Pitfall 1 (lines 403-408: vocabulary/embedding matrix index mismatch — must use vocab_size = len(tokenizer.word_index) + 2)
  - 03-RESEARCH.md Pitfall 3 (lines 415-420: gensim 4.x API — use model.wv[word] not model.syn0)
  - The notebook created in Task 1
  </read_first>

  <action>
  Append the following cells to `bigru_sentiment_models.ipynb`. Each section must be preceded by its corresponding Markdown header cell.

  **Section 3 (Markdown + Code cells):**
  
  Cell 7 (Markdown): `## 3. Train Word2Vec Embeddings`
  
  Cell 8 (Markdown): `### 3.1 Tokenize Lemmatized Text`
  
  Cell 9 (Code): Tokenize lemmatized text into sentence lists:
  - sentences = [text.split() for text in df['lemmatized']]
  - Print: sample tokens (first 20 tokens of first review), total number of sentences, average tokens per sentence
  
  Cell 10 (Markdown): `### 3.2 Train CBOW Embeddings (sg=0)`
  
  Cell 11 (Code): CBOW training per D-01/D-02/D-03:
  - cbow_model = Word2Vec(sentences=sentences, vector_size=100, window=5, min_count=5, sg=0, workers=4, epochs=5)
  - Print: vocab size, vector shape
  - Print: cbow_model.wv.most_similar('movie', topn=5) for sanity check
  
  Cell 12 (Markdown): `### 3.3 Train Skip-Gram Embeddings (sg=1)`
  
  Cell 13 (Code): Skip-Gram training per D-01/D-02/D-03:
  - sg_model = Word2Vec(sentences=sentences, vector_size=100, window=5, min_count=5, sg=1, workers=4, epochs=5)
  - Print: vocab size, vector shape
  - Print: sg_model.wv.most_similar('movie', topn=5) for sanity check
  
  Cell 14 (Markdown): `### 3.4 Save Word2Vec Models`
  
  Cell 15 (Code): Save word2vec models:
  - import os
  - os.makedirs(MODEL_DIR, exist_ok=True)
  - cbow_model.save(f'{MODEL_DIR}/word2vec_cbow.model')
  - sg_model.save(f'{MODEL_DIR}/word2vec_sg.model')
  - Print confirmation with file sizes

  **Section 4 (Markdown + Code cells):**

  Cell 16 (Markdown): `## 4. Build Vocabulary & Embedding Matrix`

  Cell 17 (Markdown): `### 4.1 Tokenizer & Vocabulary`

  Cell 18 (Code): Fit Tokenizer on lemmatized texts (all data, standard for NLP CV per RESEARCH.md Pitfall 5):
  - tokenizer = Tokenizer(oov_token='<OOV>')
  - tokenizer.fit_on_texts(lemmatized_texts)
  - word_index = tokenizer.word_index
  - VOCAB_SIZE = len(word_index) + 2  # +2 for padding (idx 0) + OOV (idx 1) — per Pitfall 1
  - EMBEDDING_DIM = 100
  - MAXLEN = 200  # per RESEARCH.md Pitfall 6
  - Print: vocabulary size, unique words
  
  Cell 19 (Markdown): `### 4.2 Build CBOW Embedding Matrix`

  Cell 20 (Code): Build embedding matrix for CBOW:
  - def build_embedding_matrix(w2v_model, tokenizer, vocab_size, embedding_dim):
  - Create np.zeros matrix of shape (vocab_size, embedding_dim)
  - Iterate tokenizer.word_index items, copy w2v_model.wv[word] if exists, else leave zero
  - Print hit/miss coverage ratio
  - Call for CBOW: cbow_embedding_matrix = build_embedding_matrix(cbow_model, tokenizer, VOCAB_SIZE, EMBEDDING_DIM)
  
  Cell 21 (Markdown): `### 4.3 Build Skip-Gram Embedding Matrix`

  Cell 22 (Code): Same function call for Skip-Gram:
  - sg_embedding_matrix = build_embedding_matrix(sg_model, tokenizer, VOCAB_SIZE, EMBEDDING_DIM)
  - Print hit/miss coverage ratio

  Cell 23 (Markdown): `### 4.4 Define BiGRU Model Builder`

  Cell 24 (Code): Per D-05/D-06/D-07/D-08/D-09, per RESEARCH.md Pattern 2 (lines 280-313):
  ```
  def build_bigru(vocab_size, embedding_dim, embedding_matrix, maxlen=200):
      model = Sequential([
          Embedding(
              input_dim=vocab_size, output_dim=embedding_dim,
              weights=[embedding_matrix], input_length=maxlen,
              trainable=True, mask_zero=True,
          ),
          Bidirectional(GRU(128)),
          Dropout(0.5),
          Dense(1, activation='sigmoid'),
      ])
      model.compile(
          optimizer=tf.keras.optimizers.Adam(learning_rate=1e-3),
          loss='binary_crossentropy',
          metrics=['accuracy',
                   tf.keras.metrics.Precision(name='precision'),
                   tf.keras.metrics.Recall(name='recall'),
                   tf.keras.metrics.AUC(name='auc')],
      )
      return model
  ```
  
  Cell 25 (Code): Quick sanity — test build:
  - test_model = build_bigru(VOCAB_SIZE, EMBEDDING_DIM, cbow_embedding_matrix, MAXLEN)
  - test_model.summary()
  - Print: model built successfully, parameter count
  - del test_model; tf.keras.backend.clear_session()  # Per Pitfall 2 (memory management)
  </action>

  <acceptance_criteria>
  - models/word2vec_cbow.model exists and is loadable via Word2Vec.load()
  - models/word2vec_sg.model exists and is loadable via Word2Vec.load()
  - Both models have vector_size=100, window=5, min_count=5
  - cbow_model.wv.vectors.shape[1] == 100 (embedding dimension)
  - VOCAB_SIZE = len(tokenizer.word_index) + 2 (matches embedding matrix rows)
  - cbow_embedding_matrix.shape == (VOCAB_SIZE, 100) 
  - Embedding coverage hit percentage > 70% (most word2vec words in tokenizer vocabulary)
  - build_bigru() function compiles without errors
  - test_model.summary() output shows: Embedding(vocab_size, 100), Bidirectional(GRU), Dropout(0.5), Dense(1, sigmoid)
  - model.count_params() callable without error
  </acceptance_criteria>

  <verify>
  <automated>python3 -c "
  import json; nb = json.load(open('bigru_sentiment_models.ipynb'));
  cells = nb['cells'];
  # Count cells: 1 title MD + 1 config C + 1 sec1 MD + 1 imports C + 1 sec2 MD + 1 load C + 1 encode C
  # + 1 sec3 MD + 1 sub3.1 MD + 1 tokenize C + 1 sub3.2 MD + 1 cbow C + 1 sub3.3 MD + 1 sg C + 1 sub3.4 MD + 1 save C 
  # + 1 sec4 MD + 1 sub4.1 MD + 1 tokenizer C + 1 sub4.2 MD + 1 matrix C + 1 sub4.3 MD + 1 sg-matrix C + 1 sub4.4 MD + 1 builder C + 1 test C
  # = 26 cells
  assert len(cells) >= 24, f'Expected >=24 cells, got {len(cells)}';
  src = ''.join(c['source'] for c in cells if c['cell_type'] == 'code');
  assert 'sg=0' in src; assert 'sg=1' in src;
  assert 'build_embedding_matrix' in src; assert 'build_bigru' in src;
  assert 'Bidirectional(GRU(128))' in src; assert 'Dropout(0.5)' in src;
  assert 'mask_zero=True' in src; assert 'early_stop' not in src;  # Plan 02 task
  print('PASS')"</automated>
  </verify>

  <done>Both Word2Vec models trained and saved. Embedding matrices built with high coverage. BiGRU model builder function defined and verified. Notebook has 26+ cells across sections 1-4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem → Notebook | Untrusted (IMDB dataset is public, preprocessed; low risk) |
| Pip install → Colab runtime | Supply chain risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | random seed reproducibility | mitigate | Set np.random.seed(42), random.seed(42), tf.random.set_seed(42) at notebook start per D-16 |
| T-03-02 | Information Disclosure | model files on Google Drive | accept | Academic context: models contain no PII; word vectors are generic embeddings |
| T-03-03 | Denial of Service | Colab T4 OOM during 10 training runs | mitigate | Use `del model; tf.keras.backend.clear_session(); gc.collect()` after each fold per Pitfall 2 |
| T-03-SC | Tampering | pip install on Colab (gensim, tensorflow) | accept | All packages from well-known PyPI ([ASSUMED] per RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<verification>
- [ ] Notebook is valid JSON (nbformat >= 4)
- [ ] Notebook executes through Section 4 without errors on local Python (if TF+gensim available) or Colab
- [ ] models/word2vec_cbow.model and models/word2vec_sg.model exist with non-zero size
- [ ] Both .model files load cleanly via Word2Vec.load()
- [ ] build_bigru() function compiles and returns a model with correct architecture
</verification>

<success_criteria>
1. Notebook `bigru_sentiment_models.ipynb` has complete sections 1-4 with all code cells
2. Two Word2Vec models saved to MODEL_DIR (one CBOW, one Skip-Gram)
3. Both embedding matrices built with coverage >70%
4. BiGRU model builder function defined and verified
5. All locked decisions (D-01 through D-16 for sections 1-4) implemented correctly
</success_criteria>

<output>
Create `.planning/phases/03-bigru-sentiment-models/03-01-SUMMARY.md` when done
</output>
