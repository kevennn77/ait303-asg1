---
phase: 03-bigru-sentiment-models
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - bigru_sentiment_models.ipynb
  - models/bigru_cbow_fold1.h5
  - models/bigru_cbow_fold2.h5
  - models/bigru_cbow_fold3.h5
  - models/bigru_cbow_fold4.h5
  - models/bigru_cbow_fold5.h5
  - models/bigru_sg_fold1.h5
  - models/bigru_sg_fold2.h5
  - models/bigru_sg_fold3.h5
  - models/bigru_sg_fold4.h5
  - models/bigru_sg_fold5.h5
autonomous: true
requirements:
  - SENT-09
  - SENT-10
  - SENT-11
  - SENT-12
  - SENT-13
user_setup: []
must_haves:
  truths:
    - "CBOW-BiGRU model trained across 5 StratifiedKFold splits with early stopping"
    - "Skip-Gram-BiGRU model trained across 5 StratifiedKFold splits with early stopping"
    - "Per-fold metrics (accuracy, precision, recall, F1, ROC-AUC) computed and stored"
    - "Aggregate metrics (mean ± std) displayed for both embedding types"
    - "Confusion matrix heatmaps generated per fold"
    - "ROC curves generated per fold"
    - "Comparison table: CBOW-BiGRU vs SG-BiGRU vs Phase 2 best SVM model"
    - "All 10 BiGRU models saved as .h5 files to MODEL_DIR"
    - "Memory cleared between folds to prevent OOM on T4"
  artifacts:
    - path: "bigru_sentiment_models.ipynb"
      provides: "Complete notebook with all 7 sections"
    - path: "models/bigru_cbow_fold{1-5}.h5"
      provides: "5 CBOW-BiGRU trained models (best-epoch) per fold"
    - path: "models/bigru_sg_fold{1-5}.h5"
      provides: "5 Skip-Gram-BiGRU trained models (best-epoch) per fold"
  key_links:
    - from: "bigru_sentiment_models.ipynb"
      to: "models/bigru_cbow_fold{1-5}.h5"
      via: "model.save() inside CV loop"
      pattern: "model\\.save\\(.*bigru_cbow"
    - from: "bigru_sentiment_models.ipynb"
      to: "models/bigru_sg_fold{1-5}.h5"
      via: "model.save() inside CV loop"
      pattern: "model\\.save\\(.*bigru_sg"
    - from: "bigru_sentiment_models.ipynb (Section 6)"
      to: "Phase 2 SVM results"
      via: "Comparison table embeds Phase 2 best model F1=0.9091"
      pattern: "0\\.9091"
---

<objective>
**Train, evaluate, and save both BiGRU classifiers (CBOW-initialized + Skip-Gram-initialized) using 5-fold StratifiedKFold on the IMDB 50K dataset.**

**Purpose:** Build on the embedding matrices from Plan 01 to produce trained BiGRU models, per-fold evaluation metrics, and a consolidated comparison with the Phase 2 SVM results.

**Output:** Complete `bigru_sentiment_models.ipynb` (sections 5-7 appended), 10 trained BiGRU .h5 model files.
</objective>

<execution_context>
@/Users/keven/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keven/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-bigru-sentiment-models/CONTEXT.md
@.planning/phases/03-bigru-sentiment-models/03-RESEARCH.md
@.planning/phases/03-bigru-sentiment-models/03-PATTERNS.md
@bigru_sentiment_models.ipynb (the file created in Plan 01)
@svm_sentiment_models.ipynb (Phase 2 for evaluation pattern + comparison table format — lines 738-773 for evaluate_model, lines 1300-1458 for comparison table)
</context>

<interfaces>
<!-- Key variables/objects from Plan 01 that this plan's code uses -->

From Plan 01 (existing notebook cells 1-25):
- `lemmatized_texts`: numpy array of lemmatized review strings (shape: 50000,)
- `y`: numpy array of encoded labels (0/1, shape: 50000,)
- `tokenizer`: fitted Tokenizer instance with word_index
- `VOCAB_SIZE`: int = len(tokenizer.word_index) + 2
- `EMBEDDING_DIM`: int = 100
- `MAXLEN`: int = 200
- `cbow_embedding_matrix`: numpy array shape (VOCAB_SIZE, 100)
- `sg_embedding_matrix`: numpy array shape (VOCAB_SIZE, 100)
- `build_bigru(vocab_size, embedding_dim, embedding_matrix, maxlen)`: function returning compiled Sequential model
- `StratifiedKFold(n_splits=5, shuffle=True, random_state=42)` — the cv object (seed=42 per D-13)

From Phase 2 notebook (svm_sentiment_models.ipynb):
- evaluate_model() pattern: returns dict with accuracy, precision, recall, f1
- Confusion matrix heatmap: sns.heatmap(cm, annot=True, fmt='d', cmap='Blues', xticklabels=['negative','positive'], yticklabels=['negative','positive'])
- Comparison table format: pd.DataFrame with Model, Accuracy, Precision, Recall, F1, CV Mean F1 columns
- Phase 2 best model: TFIDF + Lemmatized, F1=0.9091, Acc=0.9080
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement 5-fold CV training loop for CBOW-BiGRU with per-fold evaluation and model saving</name>
  <files>bigru_sentiment_models.ipynb</files>

  <read_first>
  - CONTEXT.md (Training Strategy: epochs=20, batch_size=64, Adam LR=1e-3, patience=3, restore_best_weights=True, binary_crossentropy — all locked per D-10/D-11/D-12)
  - 03-RESEARCH.md lines 317-377 (Pattern 3: 5-fold CV Training Loop — full implementation)
  - 03-RESEARCH.md Pitfall 2 (lines 409-414: OOM memory management — del model, clear_session, gc.collect() after each fold)
  - 03-RESEARCH.md Pitfall 4 (lines 422-426: EarlyStopping + model.save timing — save AFTER restore_best_weights, not during)
  - 03-PATTERNS.md lines 244-311 (CV training loop pattern + memory cleanup pattern)
  - The existing notebook from Plan 01 (all cells 1-25)
  </read_first>

  <action>
  Append the following cells to `bigru_sentiment_models.ipynb`. Each subsection requires a Markdown header before its code cell(s).

  **Section 5 (Markdown + Code cells):**

  Cell 26 (Markdown): `## 5. 5-Fold Cross-Validation & Training`
  Include note: `⚡ **HEAVY COMPUTATION** — This cell runs BiGRU training for 5-fold × 2 embedding types. Expected: ~60-90 minutes on Colab T4 GPU.`

  Cell 27 (Markdown): `### 5.1 Configuration`
  
  Cell 28 (Code): Configuration constants per D-10/D-11/D-12:
  - BATCH_SIZE = 64
  - EPOCHS = 20
  - PATIENCE = 3
  - cv = StratifiedKFold(n_splits=5, shuffle=True, random_state=42)  # Same seed as Phase 2 per D-13/D-15
  - import gc  # For memory cleanup per Pitfall 2
  - early_stop = EarlyStopping(monitor='val_loss', patience=PATIENCE, restore_best_weights=True, verbose=1)
  - Print: Configuration summary table

  Cell 29 (Markdown): `### 5.2 CBOW-BiGRU — 5-Fold CV`
  ⚡ HEAVY marker

  Cell 30 (Code): CBOW BiGRU 5-fold CV training loop.
  Per RESEARCH.md Pattern 3 (lines 321-377), adapted for CBOW:
  
  - Initialize: cbow_results = []
  - For fold, (train_idx, val_idx) in enumerate(cv.split(lemmatized_texts, y)):
    - Print fold header with separators
    - X_train_texts = [lemmatized_texts[i] for i in train_idx]; X_val_texts = [lemmatized_texts[i] for i in val_idx]
    - y_train = y[train_idx]; y_val = y[val_idx]
    - sequences_train = tokenizer.texts_to_sequences(X_train_texts)
    - sequences_val = tokenizer.texts_to_sequences(X_val_texts)
    - X_train = pad_sequences(sequences_train, maxlen=MAXLEN, padding='post', truncating='post')
    - X_val = pad_sequences(sequences_val, maxlen=MAXLEN, padding='post', truncating='post')
    - Print: shapes
    - model = build_bigru(VOCAB_SIZE, EMBEDDING_DIM, cbow_embedding_matrix, MAXLEN)
    - history = model.fit(X_train, y_train, validation_data=(X_val, y_val), epochs=EPOCHS, batch_size=BATCH_SIZE, callbacks=[early_stop], verbose=1)
    - y_pred_prob = model.predict(X_val, verbose=0).ravel()
    - y_pred = (y_pred_prob > 0.5).astype(int)
    - metrics = {
        'fold': fold+1,
        'accuracy': accuracy_score(y_val, y_pred),
        'precision': precision_score(y_val, y_pred),  # default = binary
        'recall': recall_score(y_val, y_pred),
        'f1': f1_score(y_val, y_pred),
        'roc_auc': roc_auc_score(y_val, y_pred_prob),
        'val_loss': min(history.history['val_loss']),
        'epochs_trained': len(history.history['val_loss']),
      }
    - cbow_results.append(metrics)
    - Print per-fold metrics
    - Confusion matrix heatmap per fold (identical style to Phase 2: sns.heatmap with cmap='Blues', xticklabels/yticklabels, same figsize)
    - model.save(f'{MODEL_DIR}/bigru_cbow_fold{fold+1}.h5')
    - Print model saved confirmation
    - Memory cleanup per Pitfall 2: del model; tf.keras.backend.clear_session(); gc.collect()

  Cell 31 (Code): CBOW results summary:
  - cbow_df = pd.DataFrame(cbow_results)
  - Print: per-fold table
  - Print: mean ± std for each metric
  - Print: "CBOW-BiGRU 5-fold CV complete."
  </action>

  <acceptance_criteria>
  - Notebook cell 30 (CBOW CV loop) iterates exactly 5 times (cv.split yields 5 folds)
  - Each fold: tokenizes texts, pads sequences to MAXLEN=200, builds BiGRU via build_bigru(), trains with EarlyStopping(patience=3, restore_best_weights=True), evaluates, generates confusion matrix heatmap
  - Metrics computed per fold: accuracy, precision, recall, f1, roc_auc from sklearn
  - Precision_score uses default binary averaging (same as Phase 2's evaluate_model)
  - Model saved per fold as bigru_cbow_fold{1-5}.h5
  - Memory cleanup: del model, tf.keras.backend.clear_session(), gc.collect() after each fold
  - cbow_results list populated with 5 dict entries
  - cbow_df DataFrame printed with mean ± std
  </acceptance_criteria>

  <verify>
  <automated>python3 -c "
  import json; nb = json.load(open('bigru_sentiment_models.ipynb'));
  cells = nb['cells']; src = ''.join(c['source'] for c in cells if c['cell_type'] == 'code');
  assert 'cbow_results' in src; assert 'sg_results' not in src;  # CBOW only in this task
  assert 'cv.split(lemmatized_texts, y)' in src;
  assert 'StratifiedKFold(n_splits=5, shuffle=True, random_state=42)' in src;
  assert 'restore_best_weights=True' in src;
  assert 'del model' in src; assert 'clear_session()' in src; assert 'gc.collect()' in src;
  assert 'bigru_cbow_fold' in src;
  assert 'accuracy_score(y_val, y_pred)' in src;
  assert \"precision_score(y_val, y_pred)\" in src;  # binary default
  print('PASS')"</automated>
  </verify>

  <done>CBOW-BiGRU 5-fold CV loop complete. 5 trained models saved. Per-fold metrics computed and displayed. Memory cleanup verified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Skip-Gram-BiGRU CV loop, aggregate results, comparison table, and model verification</name>
  <files>bigru_sentiment_models.ipynb</files>

  <read_first>
  - CONTEXT.md (all locked decisions apply identically for SG as CBOW)
  - 03-RESEARCH.md lines 321-377 (Pattern 3 — same loop structure as Task 1, use sg_embedding_matrix instead)
  - 03-RESEARCH.md lines 384-418 (Pattern from PATTERNS.md for aggregation + comparison table)
  - 03-PATTERNS.md lines 383-418 (Result comparison table pattern — adds ROC-AUC column)
  - 03-RESEARCH.md lines 609-644 (Example 5: evaluation metrics with confusion matrix heatmap)
  - 03-RESEARCH.md lines 646-667 (Example 6: model persistence)
  - Phase 2 notebook lines 1300-1458 (consolidated comparison table: Model, Accuracy, Precision, Recall, F1, CV Mean F1)
  - The notebook from Task 1 (contains cbow_results, cbow_df, all variables and functions)
  </read_first>

  <action>
  Append the following cells after Cell 31. All code follows identical structure to Task 1 but uses sg_embedding_matrix.

  **Section 5 continuation:**

  Cell 32 (Markdown): `### 5.3 Skip-Gram-BiGRU — 5-Fold CV`
  ⚡ HEAVY marker

  Cell 33 (Code): Skip-Gram BiGRU 5-fold CV training loop:
  - sg_results = []
  - For fold, (train_idx, val_idx) in enumerate(cv.split(lemmatized_texts, y)):
    - Identical structure to CBOW loop, except:
    - model = build_bigru(VOCAB_SIZE, EMBEDDING_DIM, sg_embedding_matrix, MAXLEN)
    - model.save(f'{MODEL_DIR}/bigru_sg_fold{fold+1}.h5')
    - All other logic identical (tokenize, pad, train, evaluate, plot confusion matrix, cleanup)
  - sg_df = pd.DataFrame(sg_results)
  - Print: per-fold table, mean ± std
  - Print: "Skip-Gram-BiGRU 5-fold CV complete."

  **Section 6 (Markdown + Code):**

  Cell 34 (Markdown): `## 6. Results & Model Comparison`

  Cell 35 (Markdown): `### 6.1 Aggregate Metrics Comparison`

  Cell 36 (Code): Aggregate comparison table:
  Build a DataFrame with rows for CBOW-BiGRU and SG-BiGRU, columns for Accuracy, Precision, Recall, F1-Score, ROC-AUC (each as "mean +/- std" formatted string using .4f). Extend RESEARCH.md's Pattern (lines 384-418) to include the Phase 2 best model as a third row for reference. For Phase 2 best, use the actual values from Phase 2 STATE.md: TFIDF + Lemmatized, F1=0.9091, Acc=0.9080. NOTE: Phase 2 did not compute ROC-AUC, so mark that cell as "N/A".

  Print comparison_df with to_string(index=False). Add a table header formatted like Phase 2: "=" * 70 and "BiGRU MODEL COMPARISON (Mean +/- Std across 5 folds)".

  Cell 37 (Markdown): `### 6.2 Loss Curves`

  Cell 38 (Code): Plot training/validation loss curves:
  - For one representative fold (fold 1 of CBOW), plot history.history['loss'] and history.history['val_loss'] on same axes
  - Title, legend, axis labels

  Cell 39 (Markdown): `### 6.3 ROC Curves`

  Cell 40 (Code): Plot ROC curves per fold for both embedding types:
  - Use sklearn.metrics.RocCurveDisplay or manual plotting
  - One plot for CBOW (5 curves), one plot for SG (5 curves)
  - Include AUC values in legend per fold

  Cell 41 (Markdown): `### 6.4 Best Model Identification`

  Cell 42 (Code): Identify best BiGRU variant:
  - Compare mean F1 of CBOW-BiGRU vs SG-BiGRU
  - Print: best BiGRU variant, mean F1, accuracy
  - Print: Phase 2 best model (TFIDF + Lemmatized, F1=0.9091) as reference
  - Print: "The best sentiment model will be used in Phase 5 for Best Buy review labeling."

  **Section 7 (Markdown + Code):**

  Cell 43 (Markdown): `## 7. Save All Models`

  Cell 44 (Code): Verify and list all model files:
  - MODEL_DIR exists
  - Iterate MODEL_DIR files, filter for .model and .h5
  - Print: file listing with sizes in MB
  - Verify 12 total files: 2 .model + 10 .h5
  - Print confirmation: "All 12 model files saved successfully."

  Cell 45 (Markdown): Final summary note: "This notebook is complete. All 4 sentiment models (2 SVM + 2 BiGRU) are trained. The best model will be used for aspect-based sentiment analysis in Phase 5."
  </action>

  <acceptance_criteria>
  - Cell 33 (SG loop) has identical structure to Cell 30 but uses sg_embedding_matrix and saves as bigru_sg_fold{1-5}.h5
  - sg_results list populated with 5 dicts
  - sg_df mean ± std printed
  - Section 6 comparison table includes: CBOW-BiGRU mean±std, SG-BiGRU mean±std, Phase 2 best (TFIDF+Lemmatized)
  - Phase 2 best values hardcoded: F1=0.9091, Accuracy=0.9080, ROC-AUC marked N/A
  - Per-fold ROC curves plotted for both embedding types
  - Loss curve plotted for representative fold
  - Cell 44 verifies 2 .model + 10 .h5 = 12 total files
  - Notebook is complete and valid JSON
  </acceptance_criteria>

  <verify>
  <automated>python3 -c "
  import json; nb = json.load(open('bigru_sentiment_models.ipynb'));
  cells = nb['cells']; src = ''.join(c['source'] for c in cells if c['cell_type'] == 'code');
  assert 'sg_results' in src; assert 'sg_embedding_matrix' in src; assert 'bigru_sg_fold' in src;
  assert 'cbow_df' in src; assert 'sg_df' in src; assert 'comparison_df' in src;
  assert '0.9091' in src; assert '0.9080' in src;  # Phase 2 best model ref
  assert 'roc_auc' in src or 'ROC-AUC' in src;
  assert 'RocCurveDisplay' in src or 'roc_curve' in src;
  assert 'model.save' in src; assert '.h5' in src;
  # Check notebook is complete: should have all 45 cells (title + config + sec1-7)
  assert len(cells) >= 44, f'Expected >=44 cells, got {len(cells)}';
  # Verify last section markdown mentions Phase 5
  last_cells = ''.join(c['source'] for c in cells[-5:]);
  assert 'Phase 5' in last_cells or 'Save All Models' in last_cells;
  print('PASS')"</automated>
  </verify>

  <done>Complete BiGRU notebook with all 7 sections. 10 BiGRU models trained, evaluated, and saved. Comparison table includes both BiGRU variants and Phase 2 reference. ROC curves, loss curves, confusion matrices generated. All 12 model files verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Colab T4 GPU memory | 10 sequential training runs risk OOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | T4 GPU OOM across 10 sequential training runs | mitigate | `del model; tf.keras.backend.clear_session(); gc.collect()` after each fold per RESEARCH.md Pitfall 2 |
| T-03-02 | Tampering | EarlyStopping restore_best_weights + model.save ordering | mitigate | Use `restore_best_weights=True` in EarlyStopping, then call `model.save()` after training completes (not inside a ModelCheckpoint callback). Per RESEARCH.md Pitfall 4. |
| T-03-03 | Repudiation | Model file overwrite | mitigate | Unique filenames per fold (`bigru_cbow_fold{1-5}.h5` and `bigru_sg_fold{1-5}.h5`). No overwrites between folds. |
| T-03-SC | Tampering | pip install on Colab | accept | All packages [ASSUMED] per RESEARCH.md Package Legitimacy Audit. No new packages beyond Plan 01. |
</threat_model>

<verification>
- [ ] Notebook is valid JSON (nbformat >= 4)
- [ ] All cells execute without errors on Colab T4 GPU
- [ ] 10 .h5 model files saved to MODEL_DIR with non-zero sizes
- [ ] CBOW-BiGRU and SG-BiGRU metrics tables printed with mean ± std
- [ ] Comparison table includes Phase 2 best model (TFIDF + Lemmatized, F1=0.9091)
- [ ] ROC curves and loss curves plotted
- [ ] Confusion matrix heatmaps generated per fold
- [ ] No more than 3 epochs of early stopping patience consumed (model converges before epoch 20)
- [ ] Memory cleanup confirmed: no OOM errors across 10 runs
</verification>

<success_criteria>
1. Complete `bigru_sentiment_models.ipynb` with all 7 sections (~45 cells)
2. 10 BiGRU .h5 model files saved (5 CBOW + 5 SG)
3. Both embedding types evaluated with 5-fold CV — mean ± std reported
4. Confusion matrices, ROC curves, loss curves generated
5. Comparison table: CBOW-BiGRU vs SG-BiGRU vs Phase 2 SVM best
6. Best BiGRU variant identified
7. All locked decisions (D-01 through D-16) implemented correctly
</success_criteria>

<output>
Create `.planning/phases/03-bigru-sentiment-models/03-02-SUMMARY.md` when done
</output>
