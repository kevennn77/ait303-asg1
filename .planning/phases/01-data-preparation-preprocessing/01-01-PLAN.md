---
plan_id: "01"
wave: 1
depends_on: []
files_modified:
  - sentiment_analysis_preprocessing.ipynb
  - data/IMDB Dataset.csv
autonomous: true
requirements:
  - SENT-01
  - CODE-01
  - CODE-02
  - CODE-03
must_haves:
  truths:
    - "Notebook opens and runs without import errors"
    - "IMDB dataset loaded into DataFrame with shape (50000, 2)"
    - "Missing values check produces 0 for both review and sentiment columns"
    - "Class balance shows exactly 25000 positive and 25000 negative reviews"
    - "Sample reviews are displayed showing raw text"
  artifacts:
    - path: "sentiment_analysis_preprocessing.ipynb"
      provides: "Sections 1 (Setup/Imports) and 2 (Load & Inspect) complete"
      min_lines: 80
      contains: "## Setup and Imports"
    - path: "data/IMDB Dataset.csv"
      provides: "Raw IMDB 50K movie review dataset"
      exists: true
  key_links:
    - from: "sentiment_analysis_preprocessing.ipynb"
      to: "data/IMDB Dataset.csv"
      via: "pd.read_csv('data/IMDB Dataset.csv')"
      pattern: "read_csv"
    - from: "sentiment_analysis_preprocessing.ipynb"
      to: "nltk.data"
      via: "nltk.download() calls in setup section"
      pattern: "nltk\.download\("
---

# Plan 01: Dataset Loading & Inspection

## Phase Goal

**As a** data analyst, **I want to** load the IMDB dataset and inspect its structure (shape, missing values, class balance), **so that** I can verify the data is clean, balanced, and ready for preprocessing.

## Objective

Create the Jupyter Notebook with environment setup, NLTK resource downloads, and the dataset loading/inspection section — producing a verifiable first slice where the raw data is visible and validated.

## Tasks

### Task 1-1-01: Environment Setup & Notebook Scaffold

<read_first>
- (greenfield — no existing notebook to read)
</read_first>

<action>
Create the Jupyter Notebook `sentiment_analysis_preprocessing.ipynb` in the project root with a new notebook (empty code cells). Then add the following to Section 1 (## Setup and Imports):

1. Set notebook metadata (kernel: python3).
2. In a markdown cell: `# AIT303 Assignment 1 — Aspect-Based Sentiment Analysis` followed by `## 1. Setup and Imports`.
3. In a code cell beneath it, place the following operations:
   - SSL context workaround for NLTK downloads (use `ssl._create_unverified_https_context` workaround pattern from RESEARCH.md).
   - Download NLTK resources with `quiet=True`: `punkt`, `stopwords`, `wordnet`, `averaged_perceptron_tagger`.
   - Import statements for: `pandas` (as `pd`), `numpy` (as `np`), `re`, `nltk`, `word_tokenize` from `nltk.tokenize`, `stopwords` from `nltk.corpus`, `PorterStemmer` and `WordNetLemmatizer` from `nltk.stem`, `pos_tag` from `nltk`, `wordnet` from `nltk.corpus`.
   - Add inline comments on each import line explaining what it provides (per CODE-01).
   - Add a print statement `print("Setup complete. NLTK resources downloaded.")` at the end of the cell.

4. Create the `data/` directory if it doesn't exist (use `os.makedirs('data', exist_ok=True)`).
5. Check if `IMDB Dataset.csv` exists in the project root and if so, move it to `data/IMDB Dataset.csv` using `shutil.move`. If the source file is already in `data/`, skip. Include a print statement showing the action taken.

Do NOT install packages via pip — just include a markdown comment at top: `# Run: pip install nltk pandas numpy jupyter` before the imports section.
</action>

<acceptance_criteria>
- `sentiment_analysis_preprocessing.ipynb` file exists in project root
- Notebook has a `## Setup and Imports` markdown section header
- All 9 imports are present: pandas, numpy, re, nltk, word_tokenize, stopwords, PorterStemmer, WordNetLemmatizer, pos_tag, wordnet
- `nltk.download()` calls exist for all 4 resources: punkt, stopwords, wordnet, averaged_perceptron_tagger
- SSL context workaround code is present
- `data/IMDB Dataset.csv` exists (moved from root or already in place)
</acceptance_criteria>

### Task 1-1-02: Load and Inspect the IMDB Dataset

<read_first>
- sentiment_analysis_preprocessing.ipynb (from Task 1-1-01 — notebook now has Section 1 complete)
</read_first>

<action>
Add Section 2 to the notebook. In the same `sentiment_analysis_preprocessing.ipynb` file:

1. Add a markdown cell: `## 2. Load and Inspect Dataset`
2. Add code cell beneath with the following operations, each on its own line with an inline comment:
   - `df = pd.read_csv('data/IMDB Dataset.csv')` — comment: "Load IMDB 50K movie reviews dataset"
   - `print(f"Shape: {df.shape}")` — displays `(50000, 2)`
   - `print(f"\nColumns: {df.columns.tolist()}")` — shows `['review', 'sentiment']`
   - `print(f"\nMissing values:\n{df.isnull().sum()}")` — should show 0 for both
   - `print(f"\nClass balance:\n{df['sentiment'].value_counts()}")` — should show 25000 positive, 25000 negative
   - `print(f"\nSentiment values: {df['sentiment'].unique()}")` — should show `['positive' 'negative']`
   - `print(f"\nSample reviews:")` then `df.head()` and `df.tail()` in separate cells for visual display

3. Add a markdown cell explaining the inspection results: note that the dataset is perfectly balanced with 25K positive and 25K negative reviews, and there are no missing values.

Use clear, descriptive variable name `df` for the DataFrame. Add inline comments per CODE-01 requirements. Use proper Python conventions (no semicolons, 4-space indentation).
</action>

<acceptance_criteria>
- Notebook has a `## 2. Load and Inspect Dataset` markdown section
- `pd.read_csv('data/IMDB Dataset.csv')` call loads the CSV with the exact path `data/IMDB Dataset.csv`
- `df.shape` output shows `(50000, 2)` when the cell runs
- `df['sentiment'].value_counts()` labels show `positive` and `negative` with counts 25000 each
- `df.isnull().sum()` shows 0 for both columns
- `df.head()` and `df.tail()` cells are present for visual inspection
- Every code line has an inline comment explaining it
</acceptance_criteria>

## Verification

```bash
# Verify notebook file exists
ls -la sentiment_analysis_preprocessing.ipynb

# Verify data file is in place
ls -la data/IMDB Dataset.csv

# Run the notebook to verify it executes
jupyter nbconvert --to notebook --execute sentiment_analysis_preprocessing.ipynb --output /tmp/test_output.ipynb
```

## must_haves

**Goal-backward check:** After this plan, a user opens the notebook and sees:
1. Section 1 (Setup) with all imports and NLTK downloads — runs clean on first execution
2. Section 2 (Load & Inspect) with the dataset loaded — raw reviews and sentiment column visible
3. Dataset properties confirmed: 50K rows, 2 columns, no missing values, perfectly balanced
4. Actual review text visible in `df.head()` output — user can read sample reviews
</must_haves>
