---
plan_id: "02"
wave: 2
depends_on:
  - "01"
files_modified:
  - sentiment_analysis_preprocessing.ipynb
autonomous: true
requirements:
  - SENT-02
  - CODE-01
  - CODE-02
  - CODE-03
must_haves:
  truths:
    - "Review text is cleaned: lowercased, HTML tags removed, non-alpha characters removed"
    - "Cleaned text has no `<br />` tags, no digits, no punctuation"
    - "Stemmed output uses PorterStemmer — 'studying' becomes 'studi', 'running' becomes 'run'"
    - "Lemmatized output uses WordNetLemmatizer with POS tagging — 'running' becomes 'run'"
    - "Stopwords ('the', 'and', 'is', 'a') are absent from both stemmed and lemmatized output"
    - "Before/after sample comparisons shown for raw → cleaned → stemmed → lemmatized"
    - "DataFrame has 4 new columns: cleaned, stemmed, lemmatized — all with (50000,) shape"
  artifacts:
    - path: "sentiment_analysis_preprocessing.ipynb"
      provides: "Complete notebook with all 5 sections — preprocessing pipeline fully implemented"
      contains: "def clean_text"
      min_lines: 200
  key_links:
    - from: "Section 3 clean_text()"
      to: "df['cleaned']"
      via: "df['review'].apply(clean_text)"
      pattern: "df\\['cleaned'\\]"
    - from: "Section 4 stem_text()"
      to: "df['stemmed']"
      via: "df['cleaned'].apply(stem_text)"
      pattern: "df\\['stemmed'\\]"
    - from: "Section 5 lemmatize_text()"
      to: "df['lemmatized']"
      via: "df['cleaned'].apply(lemmatize_text)"
      pattern: "df\\['lemmatized'\\]"
    - from: "get_wordnet_pos()"
      to: "WordNetLemmatizer.lemmatize()"
      via: "POS tag mapping for accurate lemmatization"
      pattern: "get_wordnet_pos"
---

# Plan 02: Text Preprocessing Pipeline

## Objective

Build the complete text preprocessing pipeline in the notebook — text cleaning (lowercase, HTML removal, punctuation removal, whitespace normalization), Porter stemming, and WordNet lemmatization with POS tagging. After this plan, the DataFrame has `cleaned`, `stemmed`, and `lemmatized` columns ready for downstream phases.

## Tasks

### Task 2-2-01: Text Cleaning Pipeline (Section 3)

<read_first>
- sentiment_analysis_preprocessing.ipynb (should now have Sections 1-2 from Plan 01)
</read_first>

<action>
Add Section 3 to the notebook. In `sentiment_analysis_preprocessing.ipynb`:

1. Add a markdown cell: `## 3. Text Cleaning Pipeline`
2. Add a markdown cell (sub-header): `### 3.1 Define Cleaning Function`
3. Add code cell defining `clean_text(text)` function per D-01/D-05 (notebook-only):
   - Function signature: `def clean_text(text):`
   - Docstring: `"""Clean raw review text: lowercase, remove HTML tags, remove non-alpha characters, normalize whitespace."""`
   - Body operations (sequential, each on its own line with inline comment):
     - `text = text.lower()` — comment: "Convert to lowercase"
     - `text = re.sub(r'<.*?>', '', text)` — comment: "Remove HTML tags like <br />"
     - `text = re.sub(r'[^a-zA-Z]', ' ', text)` — comment: "Remove numbers, punctuation, special characters"
     - `text = re.sub(r'\s+', ' ', text).strip()` — comment: "Normalize multiple spaces"
   - Return `text`

4. Add a markdown cell: `### 3.2 Before/After Sample`
5. Add code cell showing a sample comparison (3-5 reviews):
   - Select `df['review'].iloc[0]` for a before/after demo
   - Print with labelled sections: `Original:`, `Cleaned:` each truncated to 200 chars
   - Use f-strings for labelled output

6. Add a markdown cell: `### 3.3 Apply to Entire Dataset`
7. Add code cell applying the function:
   - `df['cleaned'] = df['review'].apply(clean_text)`
   - `print("Cleaning complete. Column 'cleaned' added to DataFrame.")`
   - `print(f"Shape: {df.shape}")` — to confirm column count increased
   - Show a progress print: `print(f"Processed {len(df)} reviews.")`

8. Add a markdown cell: `### 3.4 Verify Cleaning Results`
9. Add code cell that performs verification checks:
   - Check no HTML tags remain: `df['cleaned'].str.contains('<br').sum()` — expect 0
   - Check sample: `df[['review', 'cleaned']].head(3)`
   - Print: "HTML tag removal verified: 0 <br> tags found in cleaned column"

Add inline comments on every code line per CODE-01 requirements. Use `df` for the DataFrame per CODE-03 conventions.
</action>

<acceptance_criteria>
- `def clean_text(text):` function exists with the exact 4-step sequential pipeline
- `re.sub(r'<.*?>', '', text)` — HTML tag removal regex present
- `re.sub(r'[^a-zA-Z]', ' ', text)` — punctuation/digit removal regex present
- `re.sub(r'\s+', ' ', text).strip()` — whitespace normalization present
- `df['cleaned'] = df['review'].apply(clean_text)` applies the function to all 50K rows
- Before/after sample cell prints both original and cleaned text for at least 1 review
- Verification cell checks `<br` tag count equals 0 in cleaned column
</acceptance_criteria>

### Task 2-2-02: Stemming Pipeline (Section 4)

<read_first>
- sentiment_analysis_preprocessing.ipynb (should now have Sections 1-3 from prior tasks)
</read_first>

<action>
Add Section 4 to the notebook. In `sentiment_analysis_preprocessing.ipynb`:

1. Add a markdown cell: `## 4. Stemming Output`  <!— per D-06 —>
2. Add a markdown cell: `### 4.1 Define Stemming Function`
3. Add code cell:
   - Initialize: `stemmer = PorterStemmer()` — comment: "Porter Stemmer (algorithmic suffix stripping)"
   - Initialize: `stop_words = set(stopwords.words('english'))` — comment: "NLTK English stopwords (~179 words per D-07)"
   - Define `stem_text(text)` function:
     - Docstring: `"""Tokenize, remove stopwords, apply Porter stemming."""`
     - `tokens = word_tokenize(text)` — comment: "Tokenize into words"
     - `tokens = [t for t in tokens if t not in stop_words]` — comment: "Remove stopwords"
     - `stems = [stemmer.stem(t) for t in tokens]` — comment: "Apply Porter stemming"
     - `return ' '.join(stems)` — output space-joined string per downstream consumer requirement

4. Add a markdown cell: `### 4.2 Sample Comparison (Before/After)`
5. Add code cell:
   - Select `df['cleaned'].iloc[0]`
   - Print `Cleaned:` and `Stemmed:` with 200-char truncation

6. Add a markdown cell: `### 4.3 Apply to Dataset`
7. Add code cell:
   - `df['stemmed'] = df['cleaned'].apply(stem_text)`
   - `print(f"Stemming complete. Column 'stemmed' added. Shape: {df.shape}")`

8. Add a markdown cell: `### 4.4 Verify Stemming Effects`
9. Add code cell:
   - Print 3 sample words to show stemming effect: e.g. show token length comparison between cleaned and stemmed for first review
   - Print: `df[['review', 'cleaned', 'stemmed']].head(2)`

Add inline comments throughout per CODE-01. Use descriptive variable names (`stemmer`, `stop_words`, `tokens`, `stems`) per CODE-03.
</action>

<acceptance_criteria>
- `stemmer = PorterStemmer()` initialized with NLTK PorterStemmer per D-04
- `stop_words = set(stopwords.words('english'))` loads NLTK stopwords per D-07
- `stem_text()` function tokenizes with `word_tokenize`, filters stopwords, applies `stemmer.stem()`
- `df['stemmed'] = df['cleaned'].apply(stem_text)` applies to all rows
- Output format: space-joined string (not token list) per downstream consumer requirement
- Before/after sample printed for visual comparison
- Section header is `## 4. Stemming Output` per D-06
</acceptance_criteria>

### Task 2-2-03: Lemmatization Pipeline (Section 5)

<read_first>
- sentiment_analysis_preprocessing.ipynb (should now have Sections 1-4 complete)
</read_first>

<action>
Add Section 5 to the notebook. In `sentiment_analysis_preprocessing.ipynb`:

1. Add a markdown cell: `## 5. Lemmatization Output`  <!— per D-06 —>

2. Add a markdown cell: `### 5.1 Define POS Tag Mapper`

3. Add code cell defining `get_wordnet_pos(treebank_tag)` per D-02/D-03:
   - Docstring: `"""Map NLTK Penn Treebank POS tag to WordNet POS tag."""`
   - Body: if/elif chain:
     - `treebank_tag.startswith('J')` → return `wordnet.ADJ`
     - `treebank_tag.startswith('V')` → return `wordnet.VERB`
     - `treebank_tag.startswith('N')` → return `wordnet.NOUN`
     - `treebank_tag.startswith('R')` → return `wordnet.ADV`
     - `else` → return `wordnet.NOUN` (default)
   - Add inline comments explaining each mapping

4. Add a markdown cell: `### 5.2 Define Lemmatization Function`

5. Add code cell:
   - Initialize: `lemmatizer = WordNetLemmatizer()` — comment: "WordNet lemmatizer per D-02 (not spaCy)"
   - Initialize: `stop_words = set(stopwords.words('english'))` — comment: "NLTK stopwords per D-07"
   - Define `lemmatize_text(text)` function:
     - Docstring: `"""Tokenize, remove stopwords, POS tag, apply WordNet lemmatization with POS."""`
     - `tokens = word_tokenize(text)` — comment: "Tokenize into words"
     - `tokens = [t for t in tokens if t not in stop_words]` — comment: "Remove stopwords"
     - `pos_tags = pos_tag(tokens)` — comment: "Get POS tags per D-03 (noun/verb/adjective detection)"
     - List comprehension: `lemmas = [lemmatizer.lemmatize(token, get_wordnet_pos(pos)) for token, pos in pos_tags]` — comment: "Lemmatize each token with its POS tag"
     - `return ' '.join(lemmas)` — space-joined string

6. Add a markdown cell: `### 5.3 Sample Comparison (Before/After)`

7. Add code cell:
   - Select same sample review as in previous sections
   - Print `Cleaned:`, `Stemmed:`, `Lemmatized:` with 200-char truncation
   - This demonstrates the progressive effect and the stemming vs lemmatization difference for the report

8. Add a markdown cell: `### 5.4 Apply to Dataset`

9. Add code cell:
   - `df['lemmatized'] = df['cleaned'].apply(lemmatize_text)`
   - `print(f"Lemmatization complete. Column 'lemmatized' added. Shape: {df.shape}")`

10. Add a markdown cell: `### 5.5 Final Data Summary`

11. Add code cell:
    - Print: `print("Preprocessing complete!")`
    - Show: `print(f"Final DataFrame shape: {df.shape}")` — should be `(50000, 5)` (review, sentiment, cleaned, stemmed, lemmatized)
    - Show: `df.head(3)` for final visual confirmation
    - Print: `print(f"Columns: {df.columns.tolist()}")` — `['review', 'sentiment', 'cleaned', 'stemmed', 'lemmatized']`
    - Print token length comparison for first 3 reviews across all 4 text versions
</action>

<acceptance_criteria>
- `get_wordnet_pos()` function defined with 4 POS mappings (J→ADJ, V→VERB, N→NOUN, R→ADV) + default NOUN
- `lemmatizer = WordNetLemmatizer()` initialized (NLTK, not spaCy per D-02)
- `lemmatize_text()` function uses: word_tokenize → stopword removal → pos_tag → lemmatize with POS per D-03
- `df['lemmatized'] = df['cleaned'].apply(lemmatize_text)` applies to all rows
- Sample comparison shows ALL 4 stages: cleaned, stemmed, lemmatized side by side for at least 1 review
- Final DataFrame shape is `(50000, 5)` with columns: review, sentiment, cleaned, stemmed, lemmatized
- Output format: space-joined string (not token list)
- Section header is `## 5. Lemmatization Output` per D-06
</acceptance_criteria>

## Verification

```bash
# Execute the notebook end-to-end
jupyter nbconvert --to notebook --execute sentiment_analysis_preprocessing.ipynb --output /tmp/test_full_output.ipynb

# Verify output columns exist in executed notebook
python3 -c "
import json
with open('/tmp/test_full_output.ipynb') as f:
    nb = json.load(f)
cells = [c for c in nb['cells'] if c['cell_type'] == 'code']
print(f'Total code cells: {len(cells)}')
texts = [''.join(c['source']) for c in cells if c['source']]
print(f'Non-empty code cells: {len(texts)}')
"
```

## must_haves

**Goal-backward check:** After this plan, the user can:
1. See the complete 5-section notebook: Setup, Load & Inspect, Text Cleaning, Stemming Output, Lemmatization Output
2. Verify HTML tags are removed from cleaned text (grep for `<br` returns 0)
3. Run the notebook end-to-end without errors
4. Inspect sample reviews at each of the 4 stages (raw → cleaned → stemmed → lemmatized)
5. See stemmed output (non-words like "studi" expected — confirms Porter algorithm working)
6. See lemmatized output (real words like "study" — confirms WordNet dictionary lookup)
7. Ready the DataFrame for Phase 2 (SVM) and Phase 3 (BiGRU) consumption
</must_haves>
